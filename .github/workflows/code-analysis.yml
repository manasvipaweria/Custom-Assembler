name: Code Analysis

on:
  push:
    branches: [ "**" ]
  pull_request:
    branches: [ "**" ]
  workflow_dispatch:

jobs:
  analyze:
    uses: manasvipaweria/repo-analysis/.github/workflows/reusable-analysis.yml@main
